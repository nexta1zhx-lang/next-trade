# nextTrade 服务器运维

## 域名配置（首次部署）

### 1. DNS 解析

在域名管理后台添加 **A 记录**，指向服务器 IP：

| 类型 | 主机记录 | 值            |
| ---- | -------- | ------------- |
| A    | @        | 服务器公网 IP |
| A    | www      | 服务器公网 IP |

### 2. 首次部署（包括 Caddy）

```bash
cd ~/nextTrade

# 先拉取最新代码（包含 Caddyfile）
git pull

# 一键全量部署（会自动启动 Caddy 并申请 SSL 证书）
./scripts/deploy.sh
```

> Caddy 会自动申请和续期 Let's Encrypt SSL 证书，首次启动可能需要等待 30-60 秒。

## 日常更新代码

```bash
cd ~/nextTrade

# 一键全量部署
./scripts/deploy.sh

# 只重建 api
./scripts/deploy.sh api

# 只重建 web
./scripts/deploy.sh web

# 部署 + 数据库迁移
./scripts/deploy.sh --db-push
```

```bash
# 只重建前端
docker compose -f docker-compose.prod.yml up -d --build web

# 只重建后端
docker compose -f docker-compose.prod.yml up -d --build api
```

## 数据库表结构变更（首次部署 & 改 schema.ts 后执行）

```bash
docker compose -f docker-compose.prod.yml exec api pnpm run db:push
```

## 监控运行状态

```bash
# 所有容器运行状态（CPU、内存、端口、运行时长）
docker compose -f docker-compose.prod.yml ps

# 全局资源占用
docker stats --no-stream

# 只看单个容器资源
docker stats --no-stream nexttrade-api-1 nexttrade-web-1

# 健康检查（API 接口）
curl -s http://localhost:3001/health
```

## 查看日志

```bash
# 所有服务
docker compose -f docker-compose.prod.yml logs -f

# 只看某个服务（实时跟踪）
docker compose -f docker-compose.prod.yml logs -f api
docker compose -f docker-compose.prod.yml logs -f web
docker compose -f docker-compose.prod.yml logs -f caddy

# 只看最近 50 行（不跟踪）
docker compose -f docker-compose.prod.yml logs --tail=50 api

# 带时间戳
docker compose -f docker-compose.prod.yml logs -t -f
```

## 暂停 / 恢复

```bash
# 暂停所有（数据保留）
docker compose -f docker-compose.prod.yml down

# 恢复所有
docker compose -f docker-compose.prod.yml up -d

# 只停 web 和 api，数据库继续运行
docker compose -f docker-compose.prod.yml stop web api

# 恢复 web 和 api
docker compose -f docker-compose.prod.yml start web api

# 完全清理（⚠️ 删数据库）
docker compose -f docker-compose.prod.yml down -v
```
