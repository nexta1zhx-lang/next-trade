# 资产分析页面 — 界面功能与交互规范

> **核心需求**: 分析区数据根据走势区选定的时间区间实时重算
> **状态**: 部分已有实现，需对照本规范补齐/重构

---

## 一、页面整体布局

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 顶部控制栏: 页面标题 + 密钥/账户切换 + 计价币种 + [👁️隐私] + [🔄刷新]   │
├──────────────────────────────────────────────────────────────────────────┤
│ 1. 资产区 (全量资产分布与即时状态 — 不随下方时间段变化)                  │
├──────────────────────────────────────────────────────────────────────────┤
│ 2. 分析区 (时间范围强联动 — 展示所选区间内的收益与风控指标)              │
├──────────────────────────────────────────────────────────────────────────┤
│ 3. 走势与占比区 (可视化图表 + 时间选择控制源 + 持仓分布)                │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 二、顶部控制栏 (Top Control Bar)

### 2.1 账户/密钥切换 (Key Selector)

- **交互**: 下拉菜单或按钮组，展示当前用户所有绑定的 API Key（如 `主账户`, `7988`）
- **行为**: 切换后全局所有数据（资产区、分析区、走势图）同步刷新
- **当前状态**: ✅ 已有实现 (`selectedKeyId`)

### 2.2 计价币种切换 (Currency Switcher)

- **选项**: `USD | CNY | EUR | JPY | GBP`
- **行为**: 全页所有金额按实时汇率自动转换显示（汇率常量 `FX_RATES` 维护在前端）
- **当前状态**: ✅ 已有实现 (`currency` state)

### 2.3 资产隐藏开关 (Privacy Mode)

- **交互**: 眼睛图标 👁️ 点击切换
- **行为**: 开启后全页所有金额数字显示为 `****`，方便截图分享
- **当前状态**: ❌ 待新增

### 2.4 数据刷新与状态 (Refresh Bar)

- **信息**: 展示最后同步时间（如 `更新于 02:35`）
- **交互**: 刷新按钮 `🔄` 点击后重新拉取所有接口
- **当前状态**: ⚠️ 已有刷新按钮，缺少最后同步时间和 WebSocket 实时状态指示

---

## 三、资产区 (Asset Area)

> **定位**: 展示当前账户的**静态资产概览**，**不随**下方走势图时间段变化。反映的是最新快照的即时状态。

### 3.1 主资产卡片 (总资产)

| 元素         | 说明                                    | 当前状态               |
| ------------ | --------------------------------------- | ---------------------- |
| 总资产金额   | 大字号显示当前计价币种下的总资产        | ✅ 已有                |
| 今日振幅     | 今日最高-最低的百分比振幅               | ✅ 已有                |
| 较昨日变化   | 较前一日收盘的涨跌额 + 百分比，绿涨红跌 | ⚠️ 已有但依赖 ohlcData |
| 最后同步时间 | 显示最新快照时间                        | ✅ 已有                |

### 3.2 子账户细分网格 (Sub-Accounts Grid)

- **细分板块**: 资金 `💰` | 现货 `👛` | U合约 `📊` | 币合约 `📊` | 理财 `🐷`
- **每个卡片**:
  - 板块图标（带颜色标识）
  - 板块名称
  - 当前余额（折算后）
  - 微型占比条 (Progress Bar) — 展示该板块占总资产的权重
- **点击穿透 (Click Filter)**:
  - 点击任意板块卡片（如点击 "U合约"），该卡片高亮
  - 下方走势图**仅渲染该子账户**的历史数据（过滤对应字段）
  - 再次点击取消高亮，恢复全量视图
- **当前状态**: ⚠️ 已有网格卡片，缺少占比条和点击过滤交互

---

## 四、分析区 (Analysis Area) — ⚙️ 强联动核心

> **定位**: 数据与走势区选定的**时间段 / 框选范围**强绑定。
> 走势图时间切到"近30天"，此处所有指标瞬间重新计算为"近30天"的表现。

### 4.1 区间累计收益额 (Period PnL)

- **算法**: `区间终点资产 - 区间起点资产 - 区间内净充值`
- **联动**: 时间范围切换 → `startDate` / `endDate` 变化 → 取对应 OHLC 的 `closeVal` 计算
- **展示**: 绝对金额（法币），正值绿色负值红色
- **细分拓展**（可选）:
  - 已实现盈亏: 累加区间内平仓记录
  - 未实现盈亏: 当前持仓浮动盈亏
- **当前状态**: ❌ 当前 `cumPnl` 计算的是**全量历史**累计，非选定区间

### 4.2 区间收益率 (Period ROI)

- **算法**: `(区间终点资产 - 区间起点资产 - 区间净充值) / 区间起点资产 × 100%`
- **联动**: 同 4.1
- **展示**: 百分比，正值绿负值红
- **当前状态**: ❌ 当前 `roi` 计算的是全量历史累计 ROI

### 4.3 折算年化收益率 (Annualized ROI)

- **条件**: 选定时间区间 < 365 天
- **算法**: `ROI × (365 / 区间天数)`

  | 区间  | 天数 | ROI   | 年化   |
  | ----- | ---- | ----- | ------ |
  | 近1月 | 30   | +2.5% | +30.4% |
  | 近3月 | 90   | +8.0% | +32.4% |

- **展示**: 以较小字号跟在区间 ROI 下方，标注 `年化`
- **当前状态**: ❌ 待新增

### 4.4 区间最大回撤 (Period MDD)

- **算法**: 仅计算选定时间段内，从资产最高点到最低点的最大百分比跌幅
- **发生节点提示**: 悬浮可查看该区间内回撤发生的具体日期范围
- **联动**: 时间范围切换 → 仅扫描区间内的 OHLC 数据计算 peak-to-trough
- **当前状态**: ❌ 当前 `maxDrawdown` 计算的是全量历史 MDD

### 4.5 基准对比标签 (vs Benchmark)

- **展示**: 同一时间段内大盘（如 BTC、ETH）的表现
  ```
  区间: 近30天
  你的收益: +8.0%     BTC: +5.4%    vs BTC: +2.6%
  ```
- **数据来源**: 后端 `ticker/price` + 历史 K 线数据，或简单从 `daily-analysis` 取基准币种涨跌幅
- **当前状态**: ❌ 待新增

### 4.6 联动数据流

```
走势图时间切换 / 框选
        │
        ▼
setTimeRange() / setBrushRange(startDate, endDate)
        │
        ▼
useMemo 触发 → 从 ohlcData 筛选区间内数据
        │
        ▼
区间起点 = 区间内第一条 OHLC 的 openVal
区间终点 = 区间内最后一条 OHLC 的 closeVal
区间净充值 = getNetDeposits(startDate, endDate)
        │
        ▼
Period PnL    = 终点 - 起点 - 净充值
Period ROI    = (Period PnL / 起点) × 100%
Annualized    = ROI × (365 / 区间天数)
Period MDD    = max(peak - trough) / peak × 100%  (区间内)
vs Benchmark  = 取 BTC 同期涨跌幅对比
```

---

## 五、走势与占比区 (Charts Area)

### 5.1 资产走势图（左侧 70% 宽度）— 时间控制源

#### 5.1.1 时间粒度控制器 (Date Controls)

- **快捷按钮**: `天 | 周 | 月 | 3月 | 半年 | 一年 | 全部`
- **自定义日期范围**: 日期选择器 (Date Range Picker)，支持选择起止日期
- **控制响应**: 每次切换 → 触发 `setTimeRange()` → 分析区所有指标重新计算
- **当前状态**: ✅ 快捷按钮已有实现，缺少自定义日期范围

#### 5.1.2 单位切换 (USD / ROI%)

- **USD 模式**: 展示资产绝对金额变化趋势（默认）
- **ROI% 模式**: 展示剔除充提影响后的净值收益率曲线，起始点为 0%
- **当前状态**: ✅ 已有实现 (`chartMode`)

#### 5.1.3 图表交互 (Brush / Selection)

- **鼠标框选放大**:
  - 支持在折线图上按住鼠标拖拽框选任意时间段（如框选 6月1日-6月15日）
  - 图表局部放大到该区间
  - **分析区**同时刷新为该 15 天内的收益与回撤数据
- **重置**: 双击图表恢复当前时间粒度的完整视图
- **当前状态**: ❌ 待新增（Recharts `ReferenceArea` + `onMouseDown/onMouseUp` 实现）

#### 5.1.4 事件标记 (Milestone Markers)

- 在折线上用小图标标注：
  - 🔴 大额充值（入金 > 阈值）
  - 🔵 大额提现（出金 > 阈值）
  - 🟢 重大平仓操作
- **交互**: 悬浮显示事件详情（金额、时间、类型）
- **数据来源**: `capital_flows` 表 + 交易记录
- **当前状态**: ❌ 待新增

#### 5.1.5 模块过滤联动

- 当用户在**资产区**点击某个板块卡片（如 "U合约"）时：
  - 走势图切换为仅显示该板块的历史数据曲线
  - 分析区指标也对应仅计算该板块
  - 饼图同步高亮该板块
- **当前状态**: ❌ 待新增

### 5.2 资产占比图（右侧 30% 宽度）

#### 5.2.1 维度切换 (Tab Switcher)

- **按账户模块**: 饼图/环形图展示各板块占比（现货 vs U合约 vs 理财）
- **按币种分布**: 环形图展示具体持仓（BTC vs ETH vs USDT 等）
  - 数据来源: 最新快照的 `details` JSON 字段中的各模块币种明细
- **当前状态**: ⚠️ 按模块饼图已有，按币种分布待新增

#### 5.2.2 持仓明细悬浮

- **交互**: 鼠标 Hover 到饼图切片上
- **展示**: 对应币种/账户的持仓数量、当前均价、浮动盈亏
- **当前状态**: ⚠️ 已有基础 Tooltip，缺少明细信息

#### 5.2.3 空状态

- 无持仓时显示优雅的空状态插图 + 文字提示
- **当前状态**: ✅ 已有 `暂无数据` 空状态

---

## 六、新增后端 API

为实现完整功能，需新增以下后端接口：

### 6.1 区间分析接口

```
GET /api/asset/period-analysis?apiKeyId=&start=2026-06-01&end=2026-06-30
```

**返回**:

```json
{
  "success": true,
  "data": {
    "periodPnL": 1250.5,
    "periodROI": 8.2,
    "annualizedROI": 32.8,
    "periodMDD": -3.2,
    "mddStartDate": "2026-06-10",
    "mddEndDate": "2026-06-15",
    "netDeposit": 0,
    "benchmark": {
      "symbol": "BTC",
      "return": 5.4,
      "difference": 2.8
    }
  }
}
```

此接口可选 — 上述指标也可全由前端从已拉取的 `ohlcData` + `currentAsset` 计算，
仅在需要 `benchmark` 和 `period净充值` 时才请求后端。

### 6.2 币种持仓明细接口

```
GET /api/asset/holdings?apiKeyId=
```

返回最新快照 `details` 中各币种在现货/资金/理财模块的持仓明细。

---

## 七、数据流总结

```
┌──────────────────────────────────────────────────────────────────────────────┐
│  定时采集 (每整点 UTC)                                                       │
│  Binance SDK → collectAssetSnapshot → account_snapshots → Redis 缓存        │
└──────────────────────────────────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│  前端加载流程                                                                 │
│                                                                              │
│  fetchAll() ─┬─ /api/asset/current      → currentAssets (最新快照)           │
│              ├─ /api/asset/today         → todayData (今日分时)              │
│              └─ /api/asset/history?days= → ohlcData (日K线)                  │
│                                                                              │
│  时间范围切换 → chartData (过滤后的走势数据)                                  │
│              → periodMetrics (区间PnL/ROI/MDD) ── 分析区                      │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 八、实施优先级

| 优先级 | 功能                             | 难度 | 影响面        |
| ------ | -------------------------------- | ---- | ------------- | --- |
| P0     | 分析区改为区间计算（非全量累计） | 低   | 核心          | 2   |
| P0     | 走势图时间切换联动分析区指标     | 低   | 核心          | 2   |
| P1     | 自定义日期范围选择器             | 中   | ⭐ 走势区     | 3   |
| P1     | 资产区占比条 + 点击过滤          | 中   | 资产区/走势区 | 5   |
| P1     | 基准对比 (vs BTC)                | 中   | 分析区        | 6   |
| P2     | 框选放大 (Brush)                 | 高   | 走势区/分析区 | 7   |
| P2     | 折线图事件标记                   | 中   | 走势区        | 8   |
| P2     | 隐私模式 (金额打码)              | 低   | 全局          | 1   |
| P3     | 按币种分布饼图                   | 中   | 占比图        | 4   |
| P3     | 年化收益率                       | 低   | 分析区        | 2   |
