# nextTrade 服务器部署指南

> 适用场景：全新 Ubuntu 服务器（Docker 已安装），通过 IP 访问，无需域名和交易所 Key。

---

## 一、服务器环境准备（一次性）

### 1. 更新系统并安装 Docker

```bash
# 更新系统
sudo apt-get update
sudo apt-get install -y ca-certificates curl gnupg git

# 添加 Docker 官方 GPG 密钥
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

# 添加 Docker 官方 APT 软件源
echo \
 "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu \
 $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
 sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 安装 Docker 核心套件与 Compose 插件
sudo apt-get update
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# 免 sudo 权限设置（将当前用户加入 docker 组）
sudo usermod -aG docker $USER
```

> ⚠️ 运行后**退出并重新 SSH 登录**，组权限才能生效。

### 2. 验证安装

```bash
docker --version
docker compose version
```

---

## 二、部署项目

### 1. 克隆代码

```bash
cd ~
git clone <你的仓库地址> nextTrade
cd nextTrade
```

### 2. 创建环境变量文件

项目根目录下创建 `.env` 文件：

```bash
cat > .env << 'EOF'
# ─── JWT 签名密钥（必须更换，生产安全关键） ───
JWT_SECRET=你的随机密钥

# ─── AES-256-GCM 加密密钥（32字节 hex，必须更换） ───
ENCRYPTION_KEY=你的32字节hex密钥

# ─── CORS 允许的前端地址（填服务器 IP:3000） ───
CORS_ORIGIN=http://你的服务器IP:3000
EOF
```

生成密钥的命令（在服务器上执行）：

```bash
# 生成 JWT_SECRET（任意长随机字符串）
openssl rand -base64 32

# 生成 ENCRYPTION_KEY（32 字节 hex）
openssl rand -hex 32
```

### 3. 构建并启动所有服务

```bash
docker compose -f docker-compose.prod.yml up -d
```

首次运行会自动**构建镜像**并启动 4 个容器：
| 服务 | 镜像 | 内部端口 | 对外端口 |
|---|---|---|---|
| `postgres` | postgres:16-alpine | 5432 | 127.0.0.1:5432（仅本机） |
| `redis` | redis:7-alpine | 6379 | 127.0.0.1:6379（仅本机） |
| `api` | 本地构建 | 3001 | 0.0.0.0:3001 |
| `web` | 本地构建 | 3000 | 0.0.0.0:3000 |

### 4. 初始化数据库

```bash
# 进入 api 容器执行数据库迁移
docker compose -f docker-compose.prod.yml exec api pnpm --filter @nexttrade/api run db:push
```

---

## 三、常用操作

### 查看状态

```bash
# 查看所有容器状态
docker compose -f docker-compose.prod.yml ps

# 查看实时日志
docker compose -f docker-compose.prod.yml logs -f

# 查看单个服务日志
docker compose -f docker-compose.prod.yml logs -f api
docker compose -f docker-compose.prod.yml logs -f web
```

### 重启服务

```bash
# 重启所有
docker compose -f docker-compose.prod.yml restart

# 重启单个服务（如拉取了新代码后）
docker compose -f docker-compose.prod.yml up -d --build api
docker compose -f docker-compose.prod.yml up -d --build web
```

### 停止 / 清理

```bash
# 停止所有容器（保留数据卷）
docker compose -f docker-compose.prod.yml down

# 完全清理（⚠️ 会删除数据库数据）
docker compose -f docker-compose.prod.yml down -v
```

---

## 四、更新代码

```bash
cd ~/nextTrade
git pull
docker compose -f docker-compose.prod.yml up -d --build
```

---

## 五、访问

浏览器打开：`http://你的服务器IP:3000`

> 💡 如果需要域名 + SSL，后续可加 Nginx 反向代理 + Let's Encrypt Certbot。

---

## 六、安全建议

| 项目 | 建议 |
|---|---|
| 防火墙 | AWS 控制台安全组只放开 `3000`(Web) 端口，`22`(SSH) 限特定 IP |
| 数据库端口 | `5432`/`6379` 已绑 `127.0.0.1`，不对外暴露 |
| API 端口 `3001` | 可通过安全组限制仅内网访问，或后续走 Nginx 统一代理 |
| JWT_SECRET | 生产环境务必使用强随机值 |
| ENCRYPTION_KEY | 生产环境务必更换，丢失后已加密数据无法恢复 |
