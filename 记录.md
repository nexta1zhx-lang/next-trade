合约记录查询接口参数与限流
根据您当前所在的U本位合约账户页面和搜索结果，以下是主要的合约记录查询接口：

1. 账户成交历史 /fapi/v1/userTrades
方法: GET
权重: 20

主要参数
symbol (必填): 交易对
orderId: 订单ID（必须与symbol一起使用）
startTime: 起始时间（毫秒时间戳）
endTime: 结束时间（毫秒时间戳）
fromId: 从该交易ID开始获取数据
limit: 返回数量，默认500，最大1000
recvWindow: 请求有效时间窗口
timestamp: 签名时间戳
重要限制
时间范围: 仅支持最近6个月历史交易查询
未指定时间: 只返回最近7天数据
时间间隔: startTime和endTime最大间隔为7天
参数互斥: fromId不能与startTime/endTime同时使用
2. 限流规则
根据U本位合约的限流机制：

IP限制: 6000请求权重/分钟
订单限制: 1200订单/分钟
触发429后: 立即停止请求，等待Retry-After指定的秒数
监控响应头:
X-MBX-USED-WEIGHT-1M: 当前1分钟已使用权重
X-MBX-ORDER-COUNT-1M: 当前1分钟订单数
3. 优化建议
使用WebSocket订阅实时成交数据，减少REST查询
分批查询历史记录（每次查询不超过7天）
增加请求间隔，避免短时间密集查询
缓存已查询的数据
Sources: