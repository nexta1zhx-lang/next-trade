bitcoooin.cn {
    # 自动 HTTPS（Caddy 自动申请和续期 SSL 证书）

    # APK 等静态文件下载
    handle_path /downloads/* {
        root * /downloads
        file_server browse
    }

    # 前端所有请求转发到 Next.js
    handle {
        reverse_proxy web:3000
    }
}
