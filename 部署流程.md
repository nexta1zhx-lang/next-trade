# nextTrade 部署与发布流程

## 一、版本管理

版本信息统一在 `packages/shared/src/version.ts` 中维护：

```typescript
export const APP_VERSION = '0.1.0' // 语义版本，需手动升级
export const APP_BUILD = 1 // 构建号，每次构建自动 +1
```

| 字段          | 更新方式 | 说明                                            |
| ------------- | -------- | ----------------------------------------------- |
| `APP_VERSION` | 手动修改 | 大功能/破坏性变更时升级（如 `0.1.0` → `0.2.0`） |
| `APP_BUILD`   | 自动递增 | 每次 `pnpm apk` / `pnpm release` 自动 +1        |

### Git Tag 机制

每次构建完成后自动创建并推送 git tag，格式：`v{APP_VERSION}-b{APP_BUILD}`

```
v0.1.0-b3    ← 第一次构建
v0.1.0-b4    ← 第二次构建
v0.1.0-b5    ← 第三次构建
```

Tag 的作用：

- **精确定位 changelog 范围**：生成更新日志时，`git describe --tags` 找到最近 tag，只提取该 tag 之后的提交
- **改了版本号也不影响**：找的是最近 tag 而非特定版本，从 `0.1.0` 升到 `0.2.0` 后仍能准确定位
- **版本可追溯**：每个 APK 版本都有对应的 git 提交快照

> 如果推送失败（如网络问题），tag 会保留在本地，下次构建尝试重新推送。

---

## 二、更新日志管理

数据源唯一：`apk/changelog.json`

### 手动生成

```bash
pnpm changelog
# 交互式：输入新版本号 → 从 git log 提取提交 → 确认后写入
```

- 自动读取 `feat:` / `fix:` / `refactor:` 等 conventional commit
- 按类型归类（✨ 新功能 / 🐛 修复 / 🔧 优化）
- 提取范围：最近 git tag（如 `v0.1.0-b3`）以来的所有提交
- 同步生成 `CHANGELOG.md`

> 构建流程（`pnpm apk` / `pnpm release`）不会自动修改 changelog，
> 更新日志完全由 `pnpm changelog` 手动管理。

---

## 三、APK 构建

```bash
pnpm apk           # 生产版：构建 + 上传服务器
pnpm apk:dev       # 开发版：连本地 API，不上传
pnpm apk:upload    # 只构建不上传
```

构建产物：

```
apk/
├── changelog.json              # 更新日志（唯一数据源）
├── versions.json               # 最新版本信息
├── nexttrade-v0.1.0-b1.apk     # 每次构建保留历史版本
├── nexttrade-v0.1.0-b2.apk
└── nexttrade-v0.1.0-b3.apk
```

上传到服务器后，文件位于 `~/nextTrade/apk/`。

---

## 四、服务器部署

```bash
pnpm deploy        # 全量部署
pnpm deploy:api    # 只重建 API
pnpm deploy:web    # 只重建 Web
pnpm deploy:db     # 部署 + 数据库迁移
```

部署流程：

1. 本地检测是否在目标服务器 (`3.115.2.104`) 上
2. 不是 → SSH 到 `aws2` 远程执行
3. `git pull` 拉取最新代码
4. Docker 构建并重启服务
5. 健康检查等待 API 就绪

---

## 五、一键发布

```bash
pnpm release    # 部署服务器 + 构建 APK（顺序执行）
pnpm release:dev  # 部署 + 开发版 APK
```

完整流程：

```
                    ┌─────────────────┐
                    │  pnpm release   │
                    └────────┬────────┘
                             │
              ┌──────────────┴──────────────┐
              ▼                             ▼
    ┌─────────────────┐           ┌─────────────────┐
    │  pnpm deploy    │           │   pnpm apk      │
    │  (远程服务器)    │           │  (本地 macOS)    │
    └────────┬────────┘           └────────┬────────┘
             │                             │
             ▼                             ▼
    ┌─────────────────┐           ┌─────────────────┐
    │ git pull        │           │ build# +1       │
    │ Docker build    │           │ Next.js 构建    │
    │ 重启容器        │           │ Gradle 编译     │
    │ health check    │           │ APK → apk/      │
    └─────────────────┘           │ 创建 git tag    │
                                  │ 上传到服务器    │
                                  └─────────────────┘
```

> 更新日志请提前手动 `pnpm changelog` 生成，release 流程不会修改它。

---

## 六、发布到服务器后的文件结构

```
~/nextTrade/
├── apk/
│   ├── changelog.json               # 前端 about 页读取
│   ├── versions.json                # 前端下载链接读取
│   ├── nexttrade-v0.1.0-b3.apk      # APK 历史版本
│   └── ...
├── docker-compose.prod.yml
├── .env
└── ...
```

服务器 Caddy 配置：

- `/downloads/*` → 从服务器 `/downloads` 目录提供静态文件下载
- 需要将 `~/nextTrade/apk/` 软链接或同步到 `/downloads/`

---

## 七、更新日志示例

```json
[
  {
    "version": "0.1.0",
    "build": 1,
    "date": "2026-07-28",
    "items": [
      "🎉 初始版本发布",
      "✨ 多交易所行情聚合",
      "✨ 每日行情分析",
      "✨ 合约持仓监控",
      "✨ 资产全景分析",
      "🐛 修复服务参数初始化",
      "🔧 优化构建脚本"
    ]
  }
]
```

前端关于页面每个版本最多显示 **5 条**，超出显示 `··· 还有 N 条更新`。

---

## 八、常见问题

| 问题                         | 解决                                                                      |
| ---------------------------- | ------------------------------------------------------------------------- |
| APK 构建失败 `SDK not found` | 检查 `apps/web/android/local.properties` 中 `sdk.dir` 路径                |
| 部署 SSH 连接失败            | 检查 `~/.ssh/config` 中 `aws2` 配置                                       |
| 关于页更新日志不显示         | 运行一次 `pnpm apk`，确保 `changelog.json` 复制到 `public/downloads/` |
| 版本号不递增                 | 检查 `version.ts` 中 `APP_BUILD` 是否被手动重置                           |
